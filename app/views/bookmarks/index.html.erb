<turbo-frame id="bookmark_1">
  <%# <%= button_to {<i class="btn fa-regular fa-bookmark text-primary"></i>}, bookmarks_path, method: :post, params: {  :user_id => current_user.id, :workshop_id=> workshop.id } %>
  <%= button_to bookmarks_path, method: :post, params: {  :user_id => current_user.id, :workshop_id=> params[:id] } do %>
    <i class="fa-regular fa-bookmark text-primary" style="font-size: 20px;"></i>
  <% end %>
</turbo-frame>
